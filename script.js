function startGame(step) {
    console.log(`Başladı: ${step}'er sayma oyunu`);
    alert(`${step}'er sayma oyunu yakında başlayacak!`);
    // Buraya oyun sayfasına yönlendirme veya içerik yükleme gelebilir
    // Örnek: window.location.href = `oyun-${step}.html`;
}